# src/wsm/

Future home of the slim World State Manager C++ (RE_Kenshi plugin) and any shared headers.

**v0.1 target:** enable cvar, tick no-op, debug menu stub, MutateEconomy + snapshot stubs.

No game DLL binaries committed.
